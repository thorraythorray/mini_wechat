/**
 * 小程序配置文件
 */

// 主机域名
const host = 'https://localhost.com'
const config = {
  // 下面为具体Api地址
  host,
  requestUrl: `https://localhost.com/`,
  imgUrl: `https://localhost.com/`
}

module.exports = config